Pill action button; every Surge button is a pill — never a rounded rect.
```jsx
<Button variant="primary">Export</Button>
<Button variant="brand" caps>Book a demo</Button>
```
Variants: primary (blue, in-product), brand (black, marketing CTA — pair with caps), ghost, success. Press scales to .97.
