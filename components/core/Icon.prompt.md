Brand glyphs. Illustrative tiles use pale blue at 48–64px (waveform, scan, documents); UI marks (check, arrowUp, pause) render white inside colored circles.
```jsx
<Icon name="waveform" size={56}/>
<Icon name="check" size={14} color="#fff"/>
```
For anything not in this set, use Lucide (2px rounded stroke) from CDN.
