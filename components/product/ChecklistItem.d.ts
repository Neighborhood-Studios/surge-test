/** Follow-up question row: blue check circle + gray question text. */
export interface ChecklistItemProps{
  children:React.ReactNode;
  checked?:boolean;
  /** ms stagger for entrance animation */
  delay?:number;
}
